# Refactor, Lint Fixes, and Security Cleanup

## Goal
To clean up technical debt accumulated from the previous day's work, fix all ESLint warnings in the dashboard, update documentation, refine UI/UX (e.g., replace `<img>` with Next.js `<Image />`), and perform a code/security review to patch any issues.

## Scope
1. Fix ESLint warnings across tests and app files (unused variables, hook dependency arrays).
2. Refactor UI components to use Next.js best practices (e.g., `<Image />`).
3. Refactor duplicate code introduced in previous tracks.
4. Security review on the project's dependencies and recent code changes.
5. Update `tech-debt.md` and `lessons-learned.md`.

## Out of Scope
Adding new major features.